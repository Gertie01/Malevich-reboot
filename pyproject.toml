[project]
name = "rudalle-malevich"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "flask",
  "flask-cors",
  "rudalle==1.1.3",
  "torch",
  "transformers",
  "pillow",
]

[tool.uv]
extra-build-dependencies = { youtokentome = ["Cython"] }
override-dependencies = ["PyWavelets>=1.5.0"]
